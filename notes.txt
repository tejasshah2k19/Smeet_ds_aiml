NumPy 

    numerical python 

    python library used for numerical computing 

    we work with arrays , matrices -- efficiently 


pip install numpy 

pip3 install numpy 

python -m pip install numpy 



import numpy as np  



Task : 
1) create numpy array that contains 1 to 20 numbers [ arrOne ]. 
2) now find the min max average of arrOne. 
3) create 4*4 identity matrix. 
4) now create 1 more 1D array of size 10 with values between 1 to 100  [ arrTwo ].
5) reshape arrTwo with 2D array of 2 rows and 5 cols. 



6) find all the even numbers from arrOne and arrTwo. 
7) find the mean of each row in arrTwo. 
8) calculate the square of each elements in arrOne and arrTwo. 
9) now reverse arrOne. 




data types: numpy => C + Fortan  {approx 19 types }

int_  => int64 
int32 int64

bool_  True False 

intc  its int of c lang. 

int8  [ -128 to 127 ]
int16 [ -32768 to 32767 ]
int32 int64

uint8    [0 to 255 ]  unsigned - only positive 
uint16   [ 0 to 65535 ]
uint32
uint64


float_ => float64 

float16
float32


complex_ => complex128
complex64
complex128 


a = np.array([1,2,3,4])
print(a.dtype)


a = np.array([1,2,3,4],dtype=np.int32)
print(a.dtype)



a = np.array([1,2,3,4])
print(a.dtype) #int64 

b = np.int32(a)
print(b.dtype)  #int32 
print(a.dtype)  #int64
